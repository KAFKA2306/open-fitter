# OpenFitter

「もちふぃった～」のワークフロー概念からインスピレーションを得た、独立したオープンソースのアバター衣装フィッティングツール。

Nine Gates が GPL-3 で公開したコアロジックを基盤に、RBF（放射基底関数）によるメッシュ変形とボーンポーズ転送を連携させ、ソース体型の衣装メッシュをターゲット体型へ滑らかに移し替える一連の処理（エクスポート → 変形 → 再バインド）をまとめて提供します。

## スコープと「もちふぃった～」との違い

### 基本的な位置づけ

**もちふぃった～**は、株式会社Nine Gatesが開発・販売するVRChat向けの**商用アバター衣装フィッティングツール**です。BOOTHで2,500円で販売されており、「ベリル」などのアバター製品に同梱されることもあります。高精度な自動フィッティングと簡単な操作性（多くの場合ワンボタンで変換完了）が特徴で、VRChatコミュニティで広く使用されています。

**OpenFitter**は、もちふぃった～と**同様のワークフロー概念**（変換プロファイルを用いた衣装フィッティング）を持つ、**独立したオープンソース実装**です。本プロジェクトは、Nine Gatesが公開したGPL-3ライセンスのコアロジックのみを参照し、商用バイナリへのリバースエンジニアリングは一切行わずに開発されています。

### 技術的アプローチの違い

| 観点 | もちふぃった～（商用版） | OpenFitter（本プロジェクト） |
|------|------------------------|---------------------------|
| **コアアルゴリズム** | 独自の変形プログラム（ラティス変形ベース） | RBF（放射基底関数）+ ボーンポーズ転送 |
| **実装基盤** | プロプライエタリ（内部実装は非公開） | GPL-3公開コード + 独自UI実装 |
| **データ形式** | 変換プロファイル（共通素体からの差分情報） | シェイプキーベースのRBFフィールド + ボーンデルタ行列 |
| **Blenderツール** | 公式アドオン（プロファイル作成用、GPL-3） | 独自実装のエクスポーター（GPL-3） |
| **Unityツール** | 商用パッケージ（高度な自動化） | 独自実装のコンバーター（GPL-3） |

#### アルゴリズムの相違点

**もちふぃった～**:

- 「共通素体」（TポーズとAポーズの標準体型テンプレート）を基準とした**差分情報方式**
- 内部では独自の変形プログラムを使用（ラティス変形が言及されているが、詳細は非公開）
- ウェイト転送の高度な自動化（補助骨や胸揺れなどへの対応を含む）
- BlendShapeやPhysBonesの自動転送機能

**OpenFitter**:

- Blenderの**シェイプキー**（Basis → Target）から**RBF変形フィールド**を構築
- 多重二次調和カーネル `φ(r) = √(r² + ε²)` による空間補間
- **ボーンポーズ転送**（デルタ行列計算によるTRS変換）
- Unity Burstによる高速並列計算（NativeArray + Jobs System）

### 機能スコープの違い

| 機能 | もちふぃった～ | OpenFitter |
|------|--------------|-----------|
| **基本フィッティング** | ✅ 自動・高精度 | ⚠️ 手動プロファイル作成が必要（アルファ段階） |
| **ワンボタン変換** | ✅ | ❌（複数ステップのワークフロー） |
| **公式アバター対応** | ✅（しなの、桔梗、マヌカ、ベリルなど） | ❌（公式プロファイルなし） |
| **プロファイル配布** | ✅（BOOTHで多数配布） | ❌（ユーザー自作のみ） |
| **BlendShape転送** | ✅ | 🔄（計画中） |
| **PhysBones転送** | ✅ | 🔄（計画中） |
| **補助骨ウェイト** | ✅ | 🔄（計画中） |
| **頭部フィッティング** | ❌ | ❌ |
| **商用利用** | ✅（ライセンス購入） | ✅（GPL-3準拠） |
| **ソースコード公開** | 一部（Blenderアドオンのみ） | ✅ 全体（GPL-3） |

### ワークフローの違い

**もちふぃった～のワークフロー**:

1. Unityにツールをインポート
2. 「Download & Install」でコンポーネントをインストール
3. アバターと衣装の変換プロファイルを用意（BOOTHで配布）
4. 衣装をセットして**ワンボタンで変換実行**
5. 完成（微調整は任意）

**OpenFitterのワークフロー**:

1. **Blender**でシェイプキー（Basis/Target）を作成
2. RBFフィールドとボーンポーズをJSONエクスポート
3. **Unity**で衣装プレハブにJSONを適用
4. Convert & Saveで変換プレハブを生成
5. 微調整（必要に応じて）

### ライセンスと開発方針の違い

| 項目 | もちふぃった～ | OpenFitter |
|------|--------------|-----------|
| **ライセンス** | 商用（有料） | GPL-3（オープンソース） |
| **価格** | 2,500円（単体）/ アバター同梱 | 無料 |
| **開発元** | 株式会社Nine Gates | コミュニティ（非公式） |
| **サポート** | 公式（購入者向け） | コミュニティベース |
| **更新頻度** | 定期的（商用開発） | 不定期（ボランティア） |
| **安定性** | プロダクション品質 | アルファ（実験的） |

### 互換性について

**データ形式の非互換性**:

- OpenFitterの「RBF JSONフィールド」と、もちふぃった～の「変換プロファイル」は**データ構造が異なります**
- BOOTHで配布されている変換プロファイルは、OpenFitterでは**直接使用できません**
- 同様に、OpenFitterのエクスポートデータは、もちふぃった～では読み込めません

**ワークフロー概念の類似性**:

- 両者とも「基準体型からの差分を別体型に適用する」というコンセプトは共通
- ただし、内部実装（ラティス変形 vs RBF補間）は根本的に異なる

### どちらを選ぶべきか

**もちふぃった～を推奨するケース**:

- ✅ すぐに使える高品質なフィッティングツールが必要
- ✅ 公式対応アバター（しなの、桔梗など）を使用
- ✅ BOOTHで配布されているプロファイルを活用したい
- ✅ BlendShapeやPhysBonesの自動転送が必要
- ✅ 商用サポートと定期的な更新が必要

**OpenFitterを推奨するケース**:

- ✅ オープンソースソリューションを好む
- ✅ カスタムアバター用の独自実装が必要
- ✅ RBFアルゴリズムやコードの内部動作を理解・改変したい
- ✅ GPL-3ライセンスで商用利用したい
- ✅ Marvelous Designerなどの外部ツールとの統合が必要
- ✅ コミュニティ開発に貢献したい

### 今後の展望

**現在のOpenFitterの状態**（2025-11-25時点）:

- ステータス: **アルファ（部分的動作）**
- コア機能（RBF変形、ボーンポーズ転送）は実装済み
- UIは基本的な機能のみ（本家のような高度な自動化なし）
- 実験的プロジェクトであり、プロダクション利用は非推奨

**開発ロードマップ**:

- BlendShape転送の実装
- PhysBones対応
- プロファイル作成の簡易化
- もちふぃった～との部分的データ互換性検討（技術的に可能な範囲で）

## ライセンスについて（GPL-3）

- 本リポジトリは GPL-3.0 で配布されています。改変した OpenFitter のコード（UnityスクリプトやBlenderアドオン）を配布・頒布する場合は、同じ GPL-3 でソースコードを公開し、著作権表示とライセンス告知を保持してください。
- 自作のアバターや衣装アセットなど「データ資産」は通常 GPL のコピーレフト対象ではありませんが、OpenFitter のコードをバイナリに組み込んで再頒布する場合は、組み込んだコード部分についてソース開示義務が発生します。
- 既存の GPL ヘッダーは削除せず、そのまま残してください。

***

**ステータス**：アルファ（部分的動作）  
・もちふぃった～（商用版、BOOTH配布、Unity 2022 LTS想定が多い）と比べると、機能対応は部分的で公式アセットは付属しません。  
・UIは `Window → OpenFitter → Converter` を採用（もちふぃった～のメニュー構成とは異なります）。  
・Unity 2019.4+ を最低動作ラインとして開発・検証中。  
・非公式プロジェクトのため、バグ報告・改善提案は Issue/PR ベースで歓迎します。

***

## 技術概要

### なぜRBFか（Blender・Unity・Marvelous Designerの文脈）

- **Blender**: シェイプキーで「基準形状→ターゲット形状」の頂点差分を得やすい。RBFはこの散在点データを滑らかに補間し、未知の衣装メッシュにも変形を伝播できる。
- **Unity**: Burst + Jobs + NativeArray による並列計算で、大量頂点のRBF評価を実時間に近い速度で適用可能。座標系差（Blender: +Z up / Unity: +Y up）を補正する多項式係数変換が肝。
- **Marvelous Designer (MD)**: MDで縫製・シミュレーションした衣装を体型依存のメッシュとして書き出せる。RBFとボーン転送を組み合わせれば、MD出力を別体型へ非破壊で再適用しやすい。仕上げ時はパーティクル距離を 5mm 未満に下げると品質が向上し、作業中は 20mm 程度にすると軽い。citeturn0search0
- **RBFの利点**: ボーンウエイト再計算を最小化し、細かなしわ・ドレープを維持しやすい。線形ブレンドより滑らかで、スケルトン差異にも比較的強い。MDで最初に粗いメッシュ（高いパーティクル距離）で確認してから解像度を下げると計算が速く、破綻検出もしやすい。citeturn0search1

### RBF（Radial Basis Function - 放射基底関数）とは

**定義**：
RBFは、入力点からの距離に基づいて値を返す関数です。数学的には、中心点からの距離を引数とする関数として定義されます。

```math
φ(r) = f(||x - c||)
```

ここで c は中心点、r は距離。

**一般的な用途**：

- **データ補間**：散在する測定点から連続的な関数を構築
- **機械学習**：サポートベクターマシン（SVM）のカーネル関数
- **画像処理**：画像ワープ、モーフィング
- **数値解析**：偏微分方程式の近似解法
- **3DCG**：メッシュ変形、スカルプティング

**本プロジェクトでの利用**：
異なる体型のアバター間で衣装メッシュを変形させる際、「基準体型での変形（シェイプキー）」から「任意の衣装メッシュへの変形」を補間計算するために使用。制御点（変形前後の頂点ペア）を基に、空間全体の変形フィールドを滑らかに推定します。

**使用カーネル**：
本実装では**多重二次調和カーネル**（Multi-Quadratic Biharmonic）を採用：

```math
φ(r) = √(r² + ε²)
```

このカーネルは滑らかで安定した補間を提供し、3Dメッシュ変形に適しています。

**座標系補正（Blender → Unity）**：

- 位置: `(x_b, y_b, z_b) → (-x_b, z_b, -y_b)`
- 多項式項の符号・軸入れ替え: `p1 → -p1`, `p2 ↔ p3` かつ Z 側に符号反転
- 回転: Blender (XYZ intrinsic) から Unity Quaternion へ変換した後、`(x, y, z, w) → (x, -z, y, w)`

### ボーンポーズ転送とは

アーマチュア（ボーン構造）の姿勢差分を、異なるリグ間で転送する技術。ソースアーマチュアとターゲットアーマチュアの対応ボーンごとに、ワールド空間での変形（移動・回転・スケール）を計算し、適用します。

**Marvelous Designerとの合わせ技**：

- MDで作成した衣装をベース体型でシミュレーション → Blenderでターゲット体型との差分をシェイプキー化 → RBFエクスポート → Unityで別体型アバターに適用、という流れで、MD特有のしわ・ドレープをできるだけ保持したまま転送できる。

### 補足: ワークフローと数式の実務ポイント

- **MDのリターゲットは重みマップ補間が前提**。MD10以降のオートフィットはアバター間の重みを補間して再計算するため、標準化されたベースメッシュが必要になる。自前でやる場合も「基準体型の重み＋RBFで変形後のポーズ」という二段構成を意識すると破綻が少ない。citeturn0search0
- **シミュレーション解像度と後段リメッシュ**。MDはパーティクル距離を下げるほど滑らかだが計算コストが跳ね上がる。最終出力を別ツールで再分割・リトポする運用（低解像度で形状決定→CPU高品質パス→クアッド再メッシュ→外部でサブディバイド）が推奨。citeturn0search1
- **Blender形状キー編集の基本手順**。問題ポーズを作り、Basisと新規キーを切り替えつつ編集し、ドライバーで値を滑らかに制御するのが公式推奨フロー。citeturn0search4
- **Shape Keyを含むメッシュとモディファイア**。Blenderではシェイプキー付きメッシュにモディファイアを適用するとキーが失われるため、モディファイアはシェイプキー作成前に適用するか、エクスポート時に適用しない設定を選ぶ。citeturn0search2
- **座標系＋ポリノミアル補正を忘れない**。RBFは距離のみを扱うが、線形多項式項の軸入れ替えと符号反転を怠るとUnity側で収束しない（上記の `p1/p2/p3` 変換を適用）。
- **BlendShape/Shape Key輸送の落とし穴**。FBXやglTFでは「変形ボーンだけを書き出す」設定や名称の衝突でブレンドシェイプが無効化されるケースがある。エクスポート直前に不要ボーンを削除し、名称を一意にする。

## 機能

### Blenderツール

- **ボーンポーズエクスポーター**：2つのアーマチュア間（ソース→ターゲット）の差分をJSONファイルとしてエクスポート。
- **RBFフィールドエクスポーター**：「Basis」（基準）シェイプキーと「Target」（変形後）シェイプキーの間の変形フィールドを計算し、RBFデータ（JSON）としてエクスポート。イプシロン推定やスムージングに対応。

### Unityツール

- **OpenFitter Converter**：衣装プレハブを変換するための独立したエディタウィンドウ。
  - メッシュへのRBF変形の適用
  - アーマチュアへのボーンポーズ変形の適用
  - **アセット保存**：変形されたメッシュを自動的に保存し、すぐに使用可能なプレハブを作成。

## インストール

### Blenderアドオン

1. `blender_addon` フォルダをコピー（またはzip圧縮）し、Blenderの `Edit > Preferences > Add-ons` からインストール。
2. "Import-Export: OpenFitter Tools" を有効化。
3. **Sidebar (N-Panel) > OpenFitter** タブからツールにアクセス。

### Unityパッケージ

1. `UnityProject/Assets/OpenFitter` フォルダを、Unityプロジェクトの `Assets` フォルダ内にコピー。
2. `Newtonsoft Json` パッケージがインストールされていることを確認（最近のUnityバージョンでは通常デフォルトで含まれています。Package Managerからインストール可能）。

## 使用ワークフロー

### 1. Blender：データの準備

1. **ボーンデータ**：
    - ソースアーマチュアをターゲットアーマチュアに位置合わせ。
    - アーマチュアを選択し、**OpenFitter > Bone Pose Export** を使用して `pose_data.json` を保存。

2. **RBFデータ**：
    - 参照メッシュ上で「Basis」シェイプキー（元の形状）と「Target」シェイプキー（フィット後の形状）を作成。
    - メッシュを選択し、**OpenFitter > RBF Field Export** を使用。
    - BasisキーとTargetキーを選択し、設定（Epsilon、Smoothing）を調整して `rbf_data.json` をエクスポート。

### 2. Unity：衣装の変換

1. エクスポートされた `.json` ファイルをUnityプロジェクトにインポート。
2. **Window > OpenFitter > Converter** を開く。
3. **Source Object**（フィットさせたい衣装プレハブ）を割り当て。
4. **RBF Data JSON** と **Pose Data JSON** を割り当て。
5. **Convert & Save** をクリック。
6. 出力フォルダに新しい `[Fitted]` プレハブが作成され、すぐに使用可能。

## プロジェクト構成

```
open-fitter/
├── blender_addon/           # Blenderエクスポートツール
│   ├── rbf_exporter.py     # RBFフィールドエクスポーター
│   ├── bone_exporter.py    # ボーンポーズエクスポーター
│   └── README.md           # 技術詳細（数学・ロジック）
│
└── UnityProject/
    └── Assets/
        └── OpenFitter/      # Unity実装パッケージ
            └── Scripts/
                ├── Runtime/    # コア変形コンポーネント
                │   ├── RBFDeformer.cs
                │   ├── BoneDeformer.cs
                │   └── README.md
                └── Editor/     # エディタツール
                    ├── OpenFitterWindow.cs
                    └── README.md
```

## 技術ドキュメント

各ディレクトリに配置されたREADME.mdに、数学的詳細、ロジック、機能説明が記載されています：

- [blender_addon/README.md](blender_addon/README.md) - RBF数学、エクスポートロジック
- [UnityProject/Assets/OpenFitter/Scripts/Runtime/README.md](UnityProject/Assets/OpenFitter/Scripts/Runtime/README.md) - RBF実装、座標変換数学
- [UnityProject/Assets/OpenFitter/Scripts/Editor/README.md](UnityProject/Assets/OpenFitter/Scripts/Editor/README.md) - エディタワークフロー

## ライセンス

本プロジェクトは GNU General Public License v3.0 (GPL-3) の下で公開されています。
詳細は LICENSE ファイルをご確認ください。

## 謝辞

コアロジックをオープンソースとして公開されたNine Gatesに深く感謝いたします。オープンソースコミュニティへの多大なる貢献により、本プロジェクトの開発が可能となりました。

## クレジット・規約の遵守

本ソフトウェアは「もちふぃった～」と互換性のある、独立したオープンソース実装です。

- **コアエンジン**：Nine Gatesにより公開されたGPL-3ソースコードを基にしています。
- **UI・フロントエンド**：本プロジェクトのために独自に実装されたものです。

**開発方針と規約の遵守について**：
オリジナル製品の利用規約（特に第三条の禁止事項）を遵守するため、本プロジェクトはプロプライエタリなバイナリに対するリバースエンジニアリング（逆コンパイル、逆アセンブル等）を一切行わずに開発されました。独自実装部分は、公開されているドキュメントおよびGPL-3ソースコードのみを参照しています。

本プロジェクトは非公式なものであり、Nine Gatesおよび「もちふぃった～」からのあらゆる承認、認可等も受けていません。

## 開発状況

現在アルファステージです。コア機能は動作していますが、以下の制限があります：

- 完全な「もちふぃった～」互換性は未達成
- エッジケースでの動作が不安定な可能性
- ドキュメント整備中

Issue報告やPull Requestを歓迎します。

## システム要件

### Blender側

- Blender >= 2.80
- NumPy（Blender同梱）

### Unity側

- Unity >= 2019.4（LTS推奨）
- Newtonsoft.Json（Package Manager経由）
- Unity Burst Compiler
- Unity Mathematics
